# Copy to .env.local (git-ignored) and fill in the key for the provider you use.
# Active provider is chosen in src/api/llm.ts (default: Groq).

# https://console.groq.com/keys
GROQ_API_KEY=

# https://aistudio.google.com/apikey (only needed if you switch llm.ts to Gemini)
GEMINI_API_KEY=
