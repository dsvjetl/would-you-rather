# Copy to .env.local (git-ignored) and fill in the key for the provider you use.
# Active provider is chosen in src/api/llm.ts (default: Gemini).

# https://aistudio.google.com/apikey
GEMINI_API_KEY=

# https://console.groq.com/keys (only needed if you switch llm.ts back to Groq)
GROQ_API_KEY=
